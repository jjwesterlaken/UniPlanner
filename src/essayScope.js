/* MOVED to supabase/functions/_shared/essayScope.js so the rewrite task
   runs the scope check the tests exercise. Re-exported so existing
   imports keep working. */
export * from "../supabase/functions/_shared/essayScope.js";
